import { IssuePipePipe } from './issue-pipe.pipe';

describe('IssuePipePipe', () => {
  it('create an instance', () => {
    const pipe = new IssuePipePipe();
    expect(pipe).toBeTruthy();
  });
});
