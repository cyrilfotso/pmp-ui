import { SequentialPipe } from './sequential.pipe';

describe('SequentialPipe', () => {
  it('create an instance', () => {
    const pipe = new SequentialPipe();
    expect(pipe).toBeTruthy();
  });
});
