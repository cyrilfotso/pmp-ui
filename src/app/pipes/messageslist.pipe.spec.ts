import { MessageslistPipe } from './messageslist.pipe';

describe('MessageslistPipe', () => {
  it('create an instance', () => {
    const pipe = new MessageslistPipe();
    expect(pipe).toBeTruthy();
  });
});
